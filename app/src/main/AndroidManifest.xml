<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hitesh.ofcwork">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VerticalDatePickerPage"></activity>
        <activity android:name=".VerticalViewAdapter" />
        <activity android:name=".DatePicker" />
        <activity android:name=".FlightDatePicker" />
        <activity android:name=".RecyclerViewAdapter" />
        <activity android:name=".Flights" />
        <activity android:name=".VerticalDatePicker" />
        <activity
            android:name=".NewPassword"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ForgetPwdGetOtp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ValidateOtp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginSignUp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignUp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>